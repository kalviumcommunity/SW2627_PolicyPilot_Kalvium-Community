PolicyPilot
    ↓
RAG assistant foundation

Requirements
    Python 3.x

Setup
    Clone repository
    Create .venv
    Activate .venv
    Install requirements
    Copy .env.example → .env
    Configure environment variables
    Run application

Project Structure
    data/
    src/
    prompts/
    outputs/

Security
    .env is ignored
    API keys are never committed

Clean Run Verification
    Fresh environment tested successfully