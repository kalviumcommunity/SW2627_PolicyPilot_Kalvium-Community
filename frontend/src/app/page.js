import ChatInterface from "../components/ChatInterface";

export const metadata = {
  title: "PolicyPilot - Support Chat & Policy Query UI",
  description: "Internal e-commerce support chatbot with verified source citation attribution.",
};

export default function Home() {
  return <ChatInterface />;
}
